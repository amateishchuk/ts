import { Coord } from "../weather/shared/coordinate.model";

export class City {
        id: number;
        name: string;
        coord: Coord;
        country: string;
        population: number;
    }
